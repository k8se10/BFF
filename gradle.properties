# ---- Project coords
group=net.mod.buildcraft
version=0.1.0
archivesBaseName=buildcraft-fabric

# ---- Game / toolchain
minecraft_version=1.20.4
yarn_mappings=1.20.4+build.+     # lets Gradle pick the latest Yarn build for 1.20.4
loader_version=0.15.+            # latest 0.15.x loader
fabric_api_version=0.97.+        # latest 0.97.x Fabric API (the 1.20.4 line)

# ---- Java
org.gradle.jvmargs=-Xmx3G -XX:+UseG1GC -Dfile.encoding=UTF-8
java_version=17
